#include<stdio.h>

int main(void)
    {
        int scores[3];
        scores[0] = 73;
        scores[1] = 80;
        scores[2] = 85;
    printf("Average = %f" (scores[0] + scores[1] + scores[2])/3.0)
    }